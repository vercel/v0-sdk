# basic

1. Install dependencies: `bun install`
1. Copy `.env.example` to `.env.local` and set `V0_API_KEY`
1. Run the sync example: `bun run chats/sync.ts`
1. Run the streaming example: `bun run chats/stream.ts`
